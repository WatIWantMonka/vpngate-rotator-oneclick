# VPNGate Rotator One-Click

??? VPS ?????????? IP?
- ???? VPNGate CSV API?? `api/iphone` ????? fallback ??? HTML ?????? `do_openvpn.aspx` ??
- ??? IP ??????`no_cache`??`IP2Location + IPRegistry`
- ?????IPRegistry ???? + ???????/?????????
- ? 1 ??????????
- ? 12 ??????
- ?? API ??????/??? IP
- ?????? `vpngate`
- ????
- ?????? Linux ????apt/apk/dnf/yum/pacman/zypper?

## ??

```bash
curl -fsSL https://raw.githubusercontent.com/WatIWantMonka/vpngate-rotator-oneclick/refs/heads/main/install.sh -o install.sh
sudo bash install.sh
```

?????????
1. `IPRegistry API Key`
2. `API auth token`??? `/status` ? `/rotate`?
3. `API port`??? `18082`??????

## ?????????

```bash
vpngate status
vpngate rotate
vpngate ip
vpngate logs
vpngate token
vpngate uninstall
```

## API

- `GET /status`??? rotator ?????? Bearer Token?
- `POST /rotate`????????? IP?? Bearer Token?

???

```bash
curl -H "Authorization: Bearer <YOUR_TOKEN>" http://<VPS_IP>:<YOUR_PORT>/status
curl -X POST -H "Authorization: Bearer <YOUR_TOKEN>" http://<VPS_IP>:<YOUR_PORT>/rotate
```

## ????

- ??????????????????
- ????????????????????????? JP ???
- ? `https://www.vpngate.net/api/iphone/` ?? HTML/????? CSV ?????????? `https://www.vpngate.net/en/` ? HTML fallback?
- ?????? IP ??????
  1. `curl -4 ip.sb` ?????? IP
  2. ?? IP2Location ????
  3. ?? IPRegistry ??/????
  4. ???????PASS/FAIL?

## ??

```bash
sudo bash install.sh uninstall
# ?
vpngate uninstall
```
